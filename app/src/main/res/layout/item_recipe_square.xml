<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="140dp"
    android:layout_height="140dp"
    android:layout_marginStart="16dp">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/recipe_photo_cv"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:cardCornerRadius="8dp"
        app:cardElevation="8dp"
        app:layout_constraintBottom_toTopOf="@id/recipe_name_tv"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/recipe_photo_iv"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@color/green" />

    </com.google.android.material.card.MaterialCardView>

    <TextView
        android:id="@+id/recipe_name_tv"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="Ají parrillero"
        android:textColor="@color/black"
        android:textSize="14sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@id/recipe_photo_cv"
        app:layout_constraintStart_toStartOf="@+id/recipe_photo_cv"
        app:layout_constraintTop_toBottomOf="@id/recipe_photo_cv" />

</androidx.constraintlayout.widget.ConstraintLayout>